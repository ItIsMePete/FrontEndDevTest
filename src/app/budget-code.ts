export interface BudgetCode{
    results: string
    data: [
        budgetCodeId: number,
        fiscalYear: number,
        budgetCode: string,
        budgetTitle: string,
    ]
}

/* export interface BudgetCode{
    budgetCodeId: number
    fiscalYear: number
    budgetCode: string
    budgetTitle: string
} */
